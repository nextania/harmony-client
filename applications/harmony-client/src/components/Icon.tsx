import { styled } from "solid-styled-components";

const Icon = styled.div`  
    svg {
        fill: #d9d9d9;
    }
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
`;

export default Icon;